interface ImportMeta {
    env: {
        VITE_PUBLIC_URL: any;
        GITHUB_AUTH_TOKEN: string;
        NODE_ENV: "development" | "production";
        PORT?: string;
        PWD: string;
    };
}
